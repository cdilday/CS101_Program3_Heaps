/* This will house the insertion sort function*/
#include <stdio.h>

/* This will sort the number of elements in numkeys and put them in keys via insertion sort*/
void insertionSort(int keys[], int numKeys);

/*this prints the array. It's not unique to insertion sort, but it's helpful to have
* and since I have to include this anyway I may as well put it in.*/
void printKeys(int keys[], int numKeys);